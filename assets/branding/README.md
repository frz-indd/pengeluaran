Taruh gambar logo di sini dengan nama `intan.png`.

Setelah file ada, jalankan:

- `flutter pub get` (pastikan Windows Developer Mode aktif supaya symlink plugin bisa dibuat)
- `flutter pub run flutter_launcher_icons`
- `flutter pub run flutter_native_splash:create`

